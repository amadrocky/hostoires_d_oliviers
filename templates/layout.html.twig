{% extends 'base.html.twig' %}

{% block header %}
	{% for label, messages in app.flashes %}
		{% for message in messages %}
			<div class="flash-{{ label }}">
				{{ message }}
			</div>
		{% endfor %}
	{% endfor %}
	<nav class="navbar navbar-expand-lg fixed-top">
		<div class="container-fluid">
			<a class="navbar-brand" href="{{ path('home') }}">Histoires d'Oliviers</a>
			<button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
				<span class="navbar-toggler-icon">
					<i class="fas fa-bars pt-1"></i>
				</span>
			</button>
			<div class="collapse navbar-collapse" id="navbarNavDropdown">
				<ul class="navbar-nav ml-auto">
					<li class="nav-item dropdown">
						<a class="nav-link dropdown-toggle" href="#" id="navbarDropdownMenuLink" role="button" data-bs-toggle="dropdown" aria-expanded="false">
							Boutique
						</a>
						<ul class="dropdown-menu" aria-labelledby="navbarDropdownMenuLink">
							<li>
								<a class="dropdown-item" href="{{ path('products_index') }}">Oliviers</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('products_index') }}">Palmiers</a>
							</li>
							<li>
								<a class="dropdown-item" href="{{ path('products_index') }}">Décorations</a>
							</li>
						</ul>
					</li>
					<li class="nav-item">
						<a class="nav-link" aria-current="page" href="{{ path('advices') }}">Conseils et entretien</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('delivery') }}">Livraison et plantation</a>
					</li>
					<li class="nav-item">
						<a class="nav-link" href="{{ path('contact_index') }}">Contact</a>
					</li>
					<li class="nav-item pt-2 cart-icon">
						{% if app.session.get('cartProductsIds')|length > 0 %}
							<i class="fas fa-circle"></i>
						{% endif %}
						<a href="{{ path('cart_index') }}"><i class="fas fa-shopping-cart"></i></a>
					</li>
					<li class="nav-item p-2">
						<a href="{{ path('app_login') }}">
							<i class="fas fa-user"></i>
						</a>
					</li>
				</ul>
			</div>
		</div>
	</nav>
{% endblock %}

{% block footer %}
	{% include 'footer.html.twig' %}
{% endblock %}
